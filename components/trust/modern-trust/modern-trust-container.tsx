
import TrustBanner from './trust-banner';
import TrustStatistics from './trust-statistics';

const ModernTrustContainer = () => {
  return (
    <section className="w-full max-w-full">
      <TrustBanner />
      <TrustStatistics />
    </section>
  );
};

export default ModernTrustContainer;
